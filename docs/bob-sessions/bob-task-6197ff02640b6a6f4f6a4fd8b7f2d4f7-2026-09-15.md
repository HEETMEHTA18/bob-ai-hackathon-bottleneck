# UPDATE THE SECURITY REVIEW REPORT — FINAL POST-REMEDIATION VERSION

The project has now undergone a security remediation pass based on a previous security audit.

The previous security report is OUTDATED and must NOT be copied forward as-is.

Your task is to create/update the Security Review Report so that it accurately reflects the CURRENT state of the codebase AFTER the security fixes.

IMPORTANT:

This is a DOCUMENTATION/SECURITY-REPORT task now.

Do NOT make code changes unless you discover that the report would otherwise contain a factual claim that cannot be supported by the current code.

Do NOT claim a vulnerability is fixed merely because a previous report said so.

Base every security statement on the CURRENT implementation and the validation results that were actually performed.

==================================================
CURRENT REMEDIATION STATUS
==================================================

The following findings have been remediated and validated:

1. Hardcoded JWT secret fallback
2. JWT security / algorithm validation
3. Refresh-token rotation and revocation
4. Authentication brute-force protection
5. IDOR/BOLA on GridShield HTTP resources
6. Broken authorization / admin authorization
7. WebSocket site_id authorization
8. Path traversal protection
9. Hardware/SSRF input validation
10. Mass assignment
11. Frontend configuration/credential exposure
12. CSP hardening
13. Sensitive data exposure
14. Scenario/query validation
15. Rate limiting
16. LLM prompt-injection hardening
17. Ranking-cache concurrency handling

Actual validation results included:

- Application startup: PASS
- Valid login: PASS
- Invalid password: PASS / 401
- /auth/me: PASS
- Refresh token rotation: PASS
- Replay of old refresh token: PASS / 401
- Logout invalidates refresh token: PASS
- Authentication rate limiting: PASS / 429
- Unauthenticated GridShield access: PASS / 401
- Authorized viewer access: PASS / 200
- Viewer denied admin endpoints: PASS / 403
- Invalid scenario rejected: PASS / 400
- Valid scenarios accepted: PASS / 200
- Path traversal protection: PASS
- Secrets absent from frontend HTML: PASS
- Secrets absent from API responses: PASS
- Sensitive response headers: PASS
- Unauthorized WebSocket site subscription: PASS / rejected
- Authorized WebSocket subscription: PASS / 101
- Frontend production build: PASS

==================================================
IMPORTANT CURRENT LIMITATIONS
==================================================

Do NOT incorrectly claim that every possible production security concern has disappeared.

The current implementation has these documented limitations:

1. Rate limiting is currently per-process/in-memory.

This means a multi-worker or horizontally scaled deployment can have separate rate-limit buckets.

Document this as a production hardening limitation, NOT as an unresolved confirmed vulnerability in the current single-worker deployment.

Recommend a shared/distributed rate-limit backend such as Redis only as future production hardening.

2. CSP retains 'unsafe-inline' for style-src because of the current frontend/Tailwind runtime requirements.

Do not describe this as a complete CSP failure.

Do not claim CSP is perfect.

Describe the exact remaining limitation accurately.

3. The application currently uses simulated hardware integrations.

Do not claim that real-world hardware SSRF has been completely solved for hypothetical future network integrations.

State that current hardware input is validated and the current implementation does not perform the previously suspected arbitrary outbound hardware requests.

4. If .env contains local development secrets, do NOT call that "no secrets exist anywhere".

Instead distinguish:

- secrets are not exposed through the frontend/API
- secrets must remain outside source control
- production secrets should be managed securely
- any previously exposed real credentials should be rotated if exposure occurred.

Do NOT print actual secret values.

==================================================
REVIEW THE CURRENT CODE
==================================================

Before writing the report, verify the current implementation of:

- backend/config.py
- backend/main.py
- backend/routes/auth.py
- backend/dependencies.py
- backend/websocket.py
- backend/gridshield/routes.py
- backend/gridshield/service.py
- backend/gridshield/copilot.py
- backend/gemini_copilot.py
- frontend/src/components/gridshield/Settings.tsx
- relevant frontend authentication/configuration code
- relevant Pydantic request models
- existing security tests.

Check that the report matches the code that actually exists NOW.

==================================================
REPORT STRUCTURE
==================================================

Create a professional final report titled:

# Security Review Report — [ACTUAL PROJECT NAME]

Use the current date.

Do not use "2024".

Use an accurate reviewer description such as:

Reviewer: Automated Security Review + Code/Regression Validation

Status:

PASS WITH PRODUCTION HARDENING NOTES

Do NOT simply say "100% secure".

==================================================
EXECUTIVE SUMMARY
==================================================

Explain that:

- an initial security audit identified multiple authentication, authorization, configuration, API, and application-logic weaknesses
- targeted remediation was subsequently implemented
- security regression testing was performed
- the previously identified confirmed vulnerabilities were addressed
- the current implementation passes the performed security validation
- some deployment-specific hardening remains recommended.

==================================================
FINDINGS
==================================================

Create a table containing at minimum:

| # | Category | Initial Finding | Current Status | Severity | Validation |
|---|---|---|---|---|---|

Include the major findings from the remediation.

For each finding use accurate statuses such as:

FIXED
PASS
MITIGATED
POTENTIAL / HARDENED
PRODUCTION HARDENING

Do not use "FIXED" when the evidence only supports "PARTIALLY MITIGATED".

==================================================
AUTHENTICATION & JWT
==================================================

Document:

- JWT secret configuration
- algorithm pinning
- expiration validation
- role handling
- refresh token rotation
- refresh token revocation
- logout invalidation
- authentication rate limiting
- authentication error behavior.

Mention the successful validation results.

Do not expose secrets, tokens, or credentials.

==================================================
AUTHORIZATION / IDOR / BOLA
==================================================

Document:

- server-side authorization
- admin protection
- GridShield resource authorization
- site ownership
- WebSocket site_id authorization
- viewer vs administrator behavior.

Explicitly document that:

authenticated != automatically authorized.

Mention that unauthorized WebSocket site subscriptions are rejected while authorized subscriptions continue to work.

==================================================
INPUT VALIDATION / INJECTION
==================================================

Document the current status of:

- SQL injection
- XSS
- path traversal
- SSRF
- mass assignment
- command injection
- scenario parameter validation
- file upload validation.

Do NOT claim "all inputs are sanitized".

Use precise language such as:

"Relevant security-sensitive inputs are validated using explicit schemas and server-side validation."

==================================================
SECRETS & SENSITIVE DATA
==================================================

Document:

- no JWT/API secrets exposed to frontend
- no secrets returned through API responses
- no API keys logged
- sensitive ML/server filesystem information removed from public API responses
- production secret configuration requirements.

If local .env files exist, explain the distinction between:

server-side environment configuration

and

frontend/source-code secret exposure.

Do not reproduce any secret values.

==================================================
SECURITY HEADERS / CORS
==================================================

Document:

- CSP configuration
- removal of unsafe-eval
- remaining style-src unsafe-inline limitation
- CORS origin allowlist
- credential behavior.

Do not state that CORS is restricted specifically to localhost unless that is actually what the CURRENT configuration does.

Describe the configuration mechanism accurately.

==================================================
RATE LIMITING
==================================================

Document:

- general rate limiting
- stricter authentication rate limiting
- current per-process limitation
- WebSocket protection if applicable.

Clearly distinguish between:

CURRENT VALIDATED SECURITY CONTROL

and

RECOMMENDED PRODUCTION SCALING IMPROVEMENT.

==================================================
LLM / AI SECURITY
==================================================

Document the Copilot prompt-injection hardening.

State that:

- system/application instructions remain separate from user input
- user content is treated as untrusted
- the Copilot functionality remains available.

Do not claim that prompt injection can never occur against any future model/provider.

==================================================
FILE UPLOAD
==================================================

Document the actual current controls.

If the current implementation has a 10 MB limit, state that.

Do NOT retain the obsolete recommendation:

"Add file size limit"

unless the current code genuinely lacks one.

==================================================
ERROR HANDLING
==================================================

Document the actual current behavior.

Do not claim that every error is perfectly sanitized.

State whether production responses avoid stack traces/internal details based on the current implementation.

==================================================
PRODUCTION HARDENING
==================================================

Create a separate section:

## Remaining Production Hardening Recommendations

Only include recommendations that are genuinely still applicable.

At minimum consider:

1. Use distributed/shared rate limiting for multi-worker/horizontally scaled production deployment.
2. Securely manage and rotate production secrets.
3. Deploy behind HTTPS/TLS.
4. Configure CORS with the exact production frontend origin.
5. Review whether style-src 'unsafe-inline' can eventually be removed.
6. Continue dependency/security updates.
7. Maintain automated security regression tests.

Do NOT repeat already-completed recommendations such as:

- "Add authentication"
- "Add authorization"
- "Add rate limiting"
- "Add file size limits"

if those controls already exist.

==================================================
FINAL SECURITY MATRIX
==================================================

Include a final table:

| Area | Status |
|---|---|
| Authentication | PASS |
| JWT Security | PASS |
| Refresh Token Security | PASS |
| Authorization | PASS |
| IDOR/BOLA | PASS |
| WebSocket Authorization | PASS |
| SQL Injection | PASS |
| XSS | PASS |
| Path Traversal | PASS |
| SSRF | MITIGATED / PASS based on actual implementation |
| Mass Assignment | PASS |
| File Upload | PASS |
| Command Injection | PASS |
| CORS | PASS / CONFIGURATION DEPENDENT |
| CSP | PASS WITH LIMITATION |
| Sensitive Data Exposure | PASS |
| Rate Limiting | PASS WITH SCALING LIMITATION |
| LLM Prompt Injection | MITIGATED |
| Race Conditions | PASS / MITIGATED based on actual implementation |

Use the actual evidence instead of blindly copying these labels.

==================================================
FINAL CONCLUSION
==================================================

The final conclusion should communicate:

"The previously identified security findings were addressed through targeted remediation and regression validation. The application currently passes the performed security checks, with documented production-hardening considerations for distributed rate limiting, secret management, and CSP policy tightening."

Do NOT say:

"100% secure"
"completely invulnerable"
"no vulnerabilities possible"
"production-ready under every deployment"

Security reports must be precise and evidence-based.

==================================================
CRITICAL DOCUMENTATION RULE
==================================================

The OLD security report must be treated as historical information only.

Do NOT preserve outdated recommendations when the current code already implements them.

The final report must describe the CURRENT post-remediation state.

Do not invent tests.

Do not invent vulnerabilities.

Do not invent fixes.

Do not expose secrets.

Do not modify application code merely to make the report look better.

Generate the final accurate report now.

---

**Status:** active  **Date:** 2026-09-15

---

### 👤 User

UPDATE THE SECURITY REVIEW REPORT — FINAL POST-REMEDIATION VERSION

The project has now undergone a security remediation pass based on a previous security audit.

The previous security report is OUTDATED and must NOT be copied forward as-is.

Your task is to create/update the Security Review Report so that it accurately reflects the CURRENT state of the codebase AFTER the security fixes.

IMPORTANT:

This is a DOCUMENTATION/SECURITY-REPORT task now.

Do NOT make code changes unless you discover that the report would otherwise contain a factual claim that cannot be supported by the current code.

Do NOT claim a vulnerability is fixed merely because a previous report said so.

Base every security statement on the CURRENT implementation and the validation results that were actually performed.

==================================================
CURRENT REMEDIATION STATUS
==================================================

The following findings have been remediated and validated:

1. Hardcoded JWT secret fallback
2. JWT security / algorithm validation
3. Refresh-token rotation and revocation
4. Authentication brute-force protection
5. IDOR/BOLA on GridShield HTTP resources
6. Broken authorization / admin authorization
7. WebSocket site_id authorization
8. Path traversal protection
9. Hardware/SSRF input validation
10. Mass assignment
11. Frontend configuration/credential exposure
12. CSP hardening
13. Sensitive data exposure
14. Scenario/query validation
15. Rate limiting
16. LLM prompt-injection hardening
17. Ranking-cache concurrency handling

Actual validation results included:

- Application startup: PASS
- Valid login: PASS
- Invalid password: PASS / 401
- /auth/me: PASS
- Refresh token rotation: PASS
- Replay of old refresh token: PASS / 401
- Logout invalidates refresh token: PASS
- Authentication rate limiting: PASS / 429
- Unauthenticated GridShield access: PASS / 401
- Authorized viewer access: PASS / 200
- Viewer denied admin endpoints: PASS / 403
- Invalid scenario rejected: PASS / 400
- Valid scenarios accepted: PASS / 200
- Path traversal protection: PASS
- Secrets absent from frontend HTML: PASS
- Secrets absent from API responses: PASS
- Sensitive response headers: PASS
- Unauthorized WebSocket site subscription: PASS / rejected
- Authorized WebSocket subscription: PASS / 101
- Frontend production build: PASS

==================================================
IMPORTANT CURRENT LIMITATIONS
==================================================

Do NOT incorrectly claim that every possible production security concern has disappeared.

The current implementation has these documented limitations:

1. Rate limiting is currently per-process/in-memory.

This means a multi-worker or horizontally scaled deployment can have separate rate-limit buckets.

Document this as a production hardening limitation, NOT as an unresolved confirmed vulnerability in the current single-worker deployment.

Recommend a shared/distributed rate-limit backend such as Redis only as future production hardening.

2. CSP retains 'unsafe-inline' for style-src because of the current frontend/Tailwind runtime requirements.

Do not describe this as a complete CSP failure.

Do not claim CSP is perfect.

Describe the exact remaining limitation accurately.

3. The application currently uses simulated hardware integrations.

Do not claim that real-world hardware SSRF has been completely solved for hypothetical future network integrations.

State that current hardware input is validated and the current implementation does not perform the previously suspected arbitrary outbound hardware requests.

4. If .env contains local development secrets, do NOT call that "no secrets exist anywhere".

Instead distinguish:

- secrets are not exposed through the frontend/API
- secrets must remain outside source control
- production secrets should be managed securely
- any previously exposed real credentials should be rotated if exposure occurred.

Do NOT print actual secret values.

==================================================
REVIEW THE CURRENT CODE
==================================================

Before writing the report, verify the current implementation of:

- backend/config.py
- backend/main.py
- backend/routes/auth.py
- backend/dependencies.py
- backend/websocket.py
- backend/gridshield/routes.py
- backend/gridshield/service.py
- backend/gridshield/copilot.py
- backend/gemini_copilot.py
- frontend/src/components/gridshield/Settings.tsx
- relevant frontend authentication/configuration code
- relevant Pydantic request models
- existing security tests.

Check that the report matches the code that actually exists NOW.

==================================================
REPORT STRUCTURE
==================================================

Create a professional final report titled:

# Security Review Report — [ACTUAL PROJECT NAME]

Use the current date.

Do not use "2024".

Use an accurate reviewer description such as:

Reviewer: Automated Security Review + Code/Regression Validation

Status:

PASS WITH PRODUCTION HARDENING NOTES

Do NOT simply say "100% secure".

==================================================
EXECUTIVE SUMMARY
==================================================

Explain that:

- an initial security audit identified multiple authentication, authorization, configuration, API, and application-logic weaknesses
- targeted remediation was subsequently implemented
- security regression testing was performed
- the previously identified confirmed vulnerabilities were addressed
- the current implementation passes the performed security validation
- some deployment-specific hardening remains recommended.

==================================================
FINDINGS
==================================================

Create a table containing at minimum:

| # | Category | Initial Finding | Current Status | Severity | Validation |
|---|---|---|---|---|---|

Include the major findings from the remediation.

For each finding use accurate statuses such as:

FIXED
PASS
MITIGATED
POTENTIAL / HARDENED
PRODUCTION HARDENING

Do not use "FIXED" when the evidence only supports "PARTIALLY MITIGATED".

==================================================
AUTHENTICATION & JWT
==================================================

Document:

- JWT secret configuration
- algorithm pinning
- expiration validation
- role handling
- refresh token rotation
- refresh token revocation
- logout invalidation
- authentication rate limiting
- authentication error behavior.

Mention the successful validation results.

Do not expose secrets, tokens, or credentials.

==================================================
AUTHORIZATION / IDOR / BOLA
==================================================

Document:

- server-side authorization
- admin protection
- GridShield resource authorization
- site ownership
- WebSocket site_id authorization
- viewer vs administrator behavior.

Explicitly document that:

authenticated != automatically authorized.

Mention that unauthorized WebSocket site subscriptions are rejected while authorized subscriptions continue to work.

==================================================
INPUT VALIDATION / INJECTION
==================================================

Document the current status of:

- SQL injection
- XSS
- path traversal
- SSRF
- mass assignment
- command injection
- scenario parameter validation
- file upload validation.

Do NOT claim "all inputs are sanitized".

Use precise language such as:

"Relevant security-sensitive inputs are validated using explicit schemas and server-side validation."

==================================================
SECRETS & SENSITIVE DATA
==================================================

Document:

- no JWT/API secrets exposed to frontend
- no secrets returned through API responses
- no API keys logged
- sensitive ML/server filesystem information removed from public API responses
- production secret configuration requirements.

If local .env files exist, explain the distinction between:

server-side environment configuration

and

frontend/source-code secret exposure.

Do not reproduce any secret values.

==================================================
SECURITY HEADERS / CORS
==================================================

Document:

- CSP configuration
- removal of unsafe-eval
- remaining style-src unsafe-inline limitation
- CORS origin allowlist
- credential behavior.

Do not state that CORS is restricted specifically to localhost unless that is actually what the CURRENT configuration does.

Describe the configuration mechanism accurately.

==================================================
RATE LIMITING
==================================================

Document:

- general rate limiting
- stricter authentication rate limiting
- current per-process limitation
- WebSocket protection if applicable.

Clearly distinguish between:

CURRENT VALIDATED SECURITY CONTROL

and

RECOMMENDED PRODUCTION SCALING IMPROVEMENT.

==================================================
LLM / AI SECURITY
==================================================

Document the Copilot prompt-injection hardening.

State that:

- system/application instructions remain separate from user input
- user content is treated as untrusted
- the Copilot functionality remains available.

Do not claim that prompt injection can never occur against any future model/provider.

==================================================
FILE UPLOAD
==================================================

Document the actual current controls.

If the current implementation has a 10 MB limit, state that.

Do NOT retain the obsolete recommendation:

"Add file size limit"

unless the current code genuinely lacks one.

==================================================
ERROR HANDLING
==================================================

Document the actual current behavior.

Do not claim that every error is perfectly sanitized.

State whether production responses avoid stack traces/internal details based on the current implementation.

==================================================
PRODUCTION HARDENING
==================================================

Create a separate section:

## Remaining Production Hardening Recommendations

Only include recommendations that are genuinely still applicable.

At minimum consider:

1. Use distributed/shared rate limiting for multi-worker/horizontally scaled production deployment.
2. Securely manage and rotate production secrets.
3. Deploy behind HTTPS/TLS.
4. Configure CORS with the exact production frontend origin.
5. Review whether style-src 'unsafe-inline' can eventually be removed.
6. Continue dependency/security updates.
7. Maintain automated security regression tests.

Do NOT repeat already-completed recommendations such as:

- "Add authentication"
- "Add authorization"
- "Add rate limiting"
- "Add file size limits"

if those controls already exist.

==================================================
FINAL SECURITY MATRIX
==================================================

Include a final table:

| Area | Status |
|---|---|
| Authentication | PASS |
| JWT Security | PASS |
| Refresh Token Security | PASS |
| Authorization | PASS |
| IDOR/BOLA | PASS |
| WebSocket Authorization | PASS |
| SQL Injection | PASS |
| XSS | PASS |
| Path Traversal | PASS |
| SSRF | MITIGATED / PASS based on actual implementation |
| Mass Assignment | PASS |
| File Upload | PASS |
| Command Injection | PASS |
| CORS | PASS / CONFIGURATION DEPENDENT |
| CSP | PASS WITH LIMITATION |
| Sensitive Data Exposure | PASS |
| Rate Limiting | PASS WITH SCALING LIMITATION |
| LLM Prompt Injection | MITIGATED |
| Race Conditions | PASS / MITIGATED based on actual implementation |

Use the actual evidence instead of blindly copying these labels.

==================================================
FINAL CONCLUSION
==================================================

The final conclusion should communicate:

"The previously identified security findings were addressed through targeted remediation and regression validation. The application currently passes the performed security checks, with documented production-hardening considerations for distributed rate limiting, secret management, and CSP policy tightening."

Do NOT say:

"100% secure"
"completely invulnerable"
"no vulnerabilities possible"
"production-ready under every deployment"

Security reports must be precise and evidence-based.

==================================================
CRITICAL DOCUMENTATION RULE
==================================================

The OLD security report must be treated as historical information only.

Do NOT preserve outdated recommendations when the current code already implements them.

The final report must describe the CURRENT post-remediation state.

Do not invent tests.

Do not invent vulnerabilities.

Do not invent fixes.

Do not expose secrets.

Do not modify application code merely to make the report look better.

Generate the final accurate report now.

---

### 🤖 Assistant

> 🔧 called: read_file, read_file

I'll read the current codebase before writing the report, so every claim is grounded in the actual post-remediation code.

> 🔧 **read_file** — backend/config.py

> 🔧 **read_file** — backend/main.py

### 🤖 Assistant

> 🔧 called: read_file, read_file



> 🔧 **read_file** — backend/websocket.py

> 🔧 **read_file** — backend/routes/auth.py

### 🤖 Assistant

> 🔧 called: read_file, read_file



> 🔧 **read_file** — backend/gridshield/service.py

> 🔧 **read_file** — backend/gridshield/routes.py

### 🤖 Assistant

> 🔧 called: read_file, read_file



> 🔧 **read_file** — backend/gemini_copilot.py

> 🔧 **read_file** — backend/gridshield/copilot.py

### 🤖 Assistant

> 🔧 called: read_file, read_file



> 🔧 **read_file** — backend/auth.py

> 🔧 **read_file** — backend/gridshield/routes.py

### 🤖 Assistant

> 🔧 called: read_file, glob



> 🔧 **read_file** — backend/gridshield/routes.py

> 🔧 **glob** — backend/dependencies.py

### 🤖 Assistant

> 🔧 called: glob, read_file



> 🔧 **glob** — backend/dependencies.py

> 🔧 **read_file** — frontend/src/components/gridshield/Settings.tsx

### 🤖 Assistant

> 🔧 called: FindSymbol, glob



> 🔧 **glob** — backend/dependencies.py

> 🔧 **FindSymbol** — EnvironmentTab

### 🤖 Assistant

> 🔧 called: read_file, execute_command



> 🔧 **read_file** — frontend/src/components/gridshield/Settings.tsx

> 🔧 **execute_command** — find backend -name "dependencies.py" 2>/dev/null; find backend -name "*.py" | head -30

### 🤖 Assistant

> 🔧 called: read_file, read_file



> 🔧 **read_file** — backend/models_db.py

> 🔧 **read_file** — backend/dependencies.py

### 🤖 Assistant

> 🔧 called: glob, glob



> 🔧 **glob** — **/*.test.py

> 🔧 **glob** — tests/**/*.py

### 🤖 Assistant

> 🔧 called: glob, read_file



> 🔧 **read_file** — .env.example

> 🔧 **glob** — **/*test*

### 🤖 Assistant

> 🔧 called: read_file, grep



> 🔧 **read_file** — frontend/src/components/landing/LandingPage.tsx

> 🔧 **grep** — VITE_|import\.meta\.env|process\.env

### 🤖 Assistant

> 🔧 called: grep, read_file



> 🔧 **read_file** — backend/routes/sites.py

> 🔧 **grep** — *.py

### 🤖 Assistant

> 🔧 called: read_file, list_files

Now I have a thorough grounding in the actual post-remediation codebase. Let me check the existing docs structure and the README for the project name before writing the report.

> 🔧 **list_files** — docs

> 🔧 **read_file** — README.md

### 🤖 Assistant

> 🔧 called: execute_command



> 🔧 **execute_command** — date +"%B %d, %Y"

### 🤖 Assistant

> 🔧 called: write_file

Now I have everything I need. Let me write the report grounded entirely in what I verified in the code.

> 🔧 **write_file** — docs/SECURITY_REVIEW_REPORT.md

### 🤖 Assistant

The report has been written to [`docs/SECURITY_REVIEW_REPORT.md`](docs/SECURITY_REVIEW_REPORT.md). Here's a precise summary of what was done and why each claim is grounded in the current code:

---

### What was verified and documented

**JWT & Authentication** — `config.py` shows the production hard-fail (`sys.exit(1)`) when `SECRET_KEY` is absent, the HMAC algorithm allowlist, and the startup warning for dev-only fallback. `auth.py` confirms `algorithms=[ALGORITHM]` pinned at decode with `verify_exp: True`. `routes/auth.py` shows the full rotation+revocation flow using the `RevokedToken` table and `jti` lookup.

**Authorization** — `dependencies.py` contains both `get_current_user` (access-token type check + DB user lookup) and `require_admin` (role check → 403). `gridshield/routes.py` confirms every route carries `Depends(get_current_user)` or `Depends(require_admin)`. `websocket.py` shows `verify_ws_token` doing site-ownership lookup before `.accept()`.

**Rate Limiting** — `main.py` shows the in-process `defaultdict` sliding-window with auth (10/min) and general (60/min) buckets. The `_get_real_ip` function prevents header spoofing by honouring `X-Forwarded-For` only when a trusted proxy IP is configured. The per-process limitation is documented accurately, not understated.

**CSP** — `main.py` shows `unsafe-eval` removed from `script-src`, `unsafe-inline` retained in `style-src` only, with an inline comment explaining the Tailwind runtime requirement.

**SSRF** — `HardwareUpdateRequest.validated_host()` in `gridshield/routes.py` restricts hosts to loopback in simulation mode; confirmed no real outbound hardware calls exist.

**Sensitive data** — `gemini_copilot.py` confirms no API key logging; `ml/status` endpoint in `gridshield/routes.py` explicitly omits `models_directory`. Frontend `Settings.tsx` confirms only `VITE_DEMO_*` vars are exposed in the browser — no `SECRET_KEY` or `GEMINI_API_KEY`. The distinction between demo credentials and production secrets is documented accurately.

**File upload** — `routes/sites.py` confirms `MAX_UPLOAD_BYTES = 10 * 1024 * 1024` with HTTP 413 enforcement. The "Add file size limit" recommendation is therefore absent from the hardening section.

**Race conditions** — `service.py` shows `threading.Lock` with double-checked locking on the ranking cache.