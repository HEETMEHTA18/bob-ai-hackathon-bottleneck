---
# IBM Bob Hackathon 2026 — Official Submission
# DO NOT modify field names. Fill all required values.

submission:
  # ── Team ──────────────────────────────────────────────────────
  team_name: "bottleneck"
  team_lead:
    name: "Heet Mehta"
    email: "heetmehta18125@gmail.com"
    github: "HEETMEHTA18"

  # ── Track ─────────────────────────────────────────────────────
  track: "U1"
  problem_statement: "Power Outage Prediction & Grid Equipment Failure Advisor"

  # ── Project ───────────────────────────────────────────────────
  project_name: "Bottleneck AI"
  tagline: "PREDICT → EXPLAIN → PRIORITIZE → POSITION"
  short_description: >
    Bottleneck is an AI-powered grid reliability platform that combines
    asset telemetry, weather forecasts, and incident history to predict
    equipment failures, rank assets by operational risk, generate
    maintenance plans, and pre-position field crews before outages occur.

  # ── Repository ────────────────────────────────────────────────
  repository_url: "https://github.com/HEETMEHTA18/bob-ai-hackathon-bottleneck"
  branch: "fixed"

  # ── Demo ──────────────────────────────────────────────────────
  demo_video_url: "https://youtu.be/placeholder-bottleneck-demo"
  live_demo_url: "NOT DEPLOYED"

  # ── Tech Stack ────────────────────────────────────────────────
  tech_stack:
    backend:
      - "Python 3.11"
      - "FastAPI"
      - "Pydantic v2"
      - "SQLAlchemy + aiosqlite"
      - "XGBoost + Scikit-Learn"
    frontend:
      - "React 18"
      - "TypeScript"
      - "Vite"
      - "Recharts"
      - "Lucide Icons"
    ai:
      - "Antigravity (primary AI agent)"
      - "Google Gemini 1.5 Flash (optional AI copilot)"
      - "Real & Deterministic ML Engine"
    infrastructure:
      - "Docker"
      - "SQLite (dev) / PostgreSQL (prod)"

  # ── IBM Bob Usage ─────────────────────────────────────────────
  ibm_bob_usage:
    primary_agent: true
    sessions_documented: "docs/BOB_ENGINEERING_LOG.md"
    summary: >
      Antigravity was used as the AI coding and development agent to build
      the Bottleneck grid reliability platform, implement backend modules,
      train ML models, and build the React/TypeScript frontend.

  # ── Source Layout ─────────────────────────────────────────────
  source_root: "src"
  backend_path: "src/backend"
  frontend_path: "src/frontend"
  setup_guide: "docs/setup-guide.md"

  # ── Key Features ──────────────────────────────────────────────
  features:
    - "Impact-aware risk scoring (failure probability × grid impact × weather × criticality × redundancy)"
    - "30-asset synthetic fleet with realistic degradation profiles"
    - "XGBoost ML engine with real failure probability & anomaly detection"
    - "Scenario simulator: Severe Storm / Heatwave / Asset Degradation / Load Surge"
    - "Crew pre-positioning by specialty and region"
    - "Grounded AI copilot for grid operations"
    - "REST API endpoints under /api/bottleneck/"

